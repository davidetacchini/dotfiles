# vim:set ft=tmux:
%hidden MODULE_NAME="agent_status"

set -gq "@catppuccin_${MODULE_NAME}_icon" "󰯉 "
set -ogqF "@catppuccin_${MODULE_NAME}_color" "#{E:@thm_peach}"
set -gq "@catppuccin_${MODULE_NAME}_text" " #($HOME/.config/tmux/scripts/agent-status.sh)"

source -F "#{d:current_file}/plugins/tmux/utils/status_module.conf"
