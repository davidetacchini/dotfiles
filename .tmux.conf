# Changes the prefix to a
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# New key binds for splitting panes
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# Starts windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1
